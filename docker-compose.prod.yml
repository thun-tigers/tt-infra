services:
  tt-auth:
    environment:
      CREATE_DEFAULT_USERS: "false"

# Production only differs through its generated environment:
# PUBLIC_BASE_URL, TIGERS_VERSION, CADDYFILE, network and volume names.
